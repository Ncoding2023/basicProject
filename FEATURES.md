# ✨ 프로젝트 기능 상세 설명

## 🎯 주요 기능

### 1. 자동 API 문서화 (Swagger/OpenAPI)

#### 특징
- ✅ JSDoc 주석으로 자동 생성
- ✅ 인터랙티브 UI (Try it out 버튼)
- ✅ 요청/응답 예제 자동 표시
- ✅ 실시간 API 테스트 가능
- ✅ 스키마 검증

#### 접근
```
http://localhost:8080/api-docs
```

#### 예시
```javascript
/**
 * @swagger
 * /api/users:
 *   get:
 *     summary: 사용자 목록 조회
 *     responses:
 *       200:
 *         description: 성공
 */
router.get('/users', (req, res) => {
  // 구현...
});
```

---

### 2. HTTP 로깅 (Morgan)

#### 특징
- ✅ 모든 요청/응답 자동 기록
- ✅ 응답 시간 표시
- ✅ HTTP 상태 코드 기록
- ✅ 터미널에서 실시간 모니터링

#### 로그 형식
```
GET /api/hello 200 - - 2.345 ms
POST /api/message 200 - - 1.234 ms
GET /api/users 200 - - 0.987 ms
```

#### 용도
- API 성능 모니터링
- 요청 추적
- 디버깅

---

### 3. 코드 품질 관리

#### ESLint (코드 검사)

**기능:**
- 코드 스타일 일관성 유지
- 잠재적 버그 조기 발견
- 우수 사례 강제

**사용:**
```bash
npm run lint        # 검사
npm run lint:fix    # 자동 수정
```

**규칙 예시:**
```javascript
// ❌ 나쁜 예
var x = 1;
const y=2;

// ✅ 좋은 예 (ESLint 적용 후)
const x = 1;
const y = 2;
```

#### Prettier (코드 포맷팅)

**기능:**
- 자동 코드 포맷팅
- 들여쓰기, 괄호 자동 정렬
- 세미콜론 일관성 유지

**사용:**
```bash
npm run format
```

**결과:**
```javascript
// 포맷팅 전
function hello(){console.log('hello')}

// 포맷팅 후
function hello() {
  console.log('hello');
}
```

---

### 4. 환경 변수 관리

#### 기능
- `.env` 파일로 민감한 정보 관리
- 환경별 설정 분리 (개발/프로덕션)
- Git에서 제외

#### 사용
```bash
# .env 파일 생성
PORT=8080
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
```

#### 접근
```javascript
const config = require('./config');
console.log(config.PORT); // 8080
```

---

### 5. API 엔드포인트

#### GET /api/hello
**설명:** 기본 테스트 엔드포인트

**응답:**
```json
{
  "message": "안녕하세요! Node.js Express에서 보낸 데이터입니다.",
  "timestamp": "2025-11-28T12:00:00.000Z",
  "version": "1.0.0"
}
```

#### GET /api/users
**설명:** 사용자 목록 조회

**응답:**
```json
{
  "users": [
    { "id": 1, "name": "사용자1", "email": "user1@example.com" },
    { "id": 2, "name": "사용자2", "email": "user2@example.com" }
  ]
}
```

#### POST /api/message
**설명:** 메시지 처리

**요청:**
```json
{
  "message": "Hello Server"
}
```

**응답:**
```json
{
  "success": true,
  "receivedMessage": "Hello Server",
  "processedAt": "2025-11-28T12:00:00.000Z"
}
```

---

### 6. React 프론트엔드 기능

#### 제공 기능
- ✅ 서버 상태 확인
- ✅ 사용자 목록 조회
- ✅ 메시지 전송
- ✅ 실시간 응답 표시
- ✅ 에러 처리
- ✅ 모던 UI/UX

#### 화면 구성
```
┌─────────────────────────────────┐
│   Node.js + React 풀스택 템플릿  │
├─────────────────────────────────┤
│                                 │
│ 🔗 서버 연결 상태                │
│  ✅ [상태 메시지]               │
│                                 │
│ 👥 사용자 목록                   │
│  [사용자 조회] 버튼              │
│  • 사용자1                       │
│  • 사용자2                       │
│                                 │
│ 📨 메시지 전송                   │
│  [입력 필드] [전송] 버튼        │
│  ✅ 전송됨: Hello               │
│                                 │
└─────────────────────────────────┘
```

---

## 🔄 개발 워크플로우

### 1. API 추가 단계

#### 단계 1: 라우트 작성
```javascript
// backend/routes/api.js

/**
 * @swagger
 * /api/todos:
 *   get:
 *     summary: 할일 목록 조회
 */
router.get('/todos', (req, res) => {
  res.json({ todos: [...] });
});
```

#### 단계 2: Swagger 자동 생성
```bash
# 서버 재시작
npm run dev
```

#### 단계 3: 문서 확인
```
http://localhost:8080/api-docs
```

#### 단계 4: 프론트엔드 연결
```javascript
// frontend/src/App.jsx
const [todos, setTodos] = useState([]);

const fetchTodos = async () => {
  const res = await fetch('http://localhost:8080/api/todos');
  const data = await res.json();
  setTodos(data.todos);
};
```

---

### 2. 코드 품질 검사

```bash
# 검사
npm run lint

# 오류 있으면 자동 수정
npm run lint:fix

# 코드 포맷팅
npm run format

# 다시 확인
npm run lint
```

---

## 📊 성능 모니터링

### Morgan 로그 분석

```
GET /api/hello 200 - - 2.345 ms     ← 빠름 ✅
POST /api/message 200 - - 15.234 ms  ← 느림 ⚠️
```

**개선 방법:**
- 데이터베이스 쿼리 최적화
- 캐싱 추가
- 비동기 작업 개선

---

## 🔐 보안

### 적용된 보안 조치
- ✅ CORS 설정 (신뢰할 수 있는 출처만 허용)
- ✅ HTTP 헤더 검증
- ✅ 입력값 검증

### 추가 권장 사항
- 🔒 HTTPS 사용
- 🔑 JWT 토큰 기반 인증
- 🛡️ Rate limiting
- 🚫 SQL Injection 방지

---

## 🚀 배포 준비 체크리스트

- [ ] 환경 변수 설정 확인
- [ ] ESLint 검사 통과
- [ ] 모든 API 테스트 완료
- [ ] Swagger 문서 완성
- [ ] 프로덕션 빌드 생성
- [ ] 보안 설정 확인
- [ ] CORS 설정 업데이트
- [ ] 에러 로깅 설정
- [ ] 성능 최적화
- [ ] 배포 스크립트 작성

---

## 📚 추가 학습 자료

- **Express.js**: https://expressjs.com/
- **Swagger/OpenAPI**: https://swagger.io/
- **React**: https://react.dev/
- **Vite**: https://vitejs.dev/
- **Morgan**: https://github.com/expressjs/morgan
- **ESLint**: https://eslint.org/
- **Prettier**: https://prettier.io/

---

**계속 개선하면서 프로젝트를 성장시켜 나가세요!** 🎉

